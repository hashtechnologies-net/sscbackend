export const selectHospitalList = (state) => state.hospital.hospitalList;
export const selectHospitalListLoading = (state) =>
  state.hospital.hospitalListLoading;
export const selectHospitalListSuccess = (state) =>
  state.hospital.hospitalListSuccess;
export const selectHospitalListError = (state) =>
  state.hospital.hospitalListError;
